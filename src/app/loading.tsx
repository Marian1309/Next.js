import { BicacleLoader } from '@/components/shared';

const Loading = () => {
  return (
    <div className="flex min-h-dvh flex-1 items-center justify-center">
      <BicacleLoader />
    </div>
  );
};

export default Loading;
